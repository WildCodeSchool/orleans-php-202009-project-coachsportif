{% extends 'layout_admin.html.twig' %}

{% block title %}Ajout d'un nouvel avis{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="title text-info text-center mt-4">Ajout d'un nouvel avis</h1>
        {% form_theme form 'bootstrap_4_layout.html.twig' %}
        {{ include('opinion/_form.html.twig') }}
            <div class="text-center">
                <a class="btn btn-secondary btn-lg m-3" href="{{ path('home') }}">Retour</a>
            </div>
    </div>
{% endblock %}

