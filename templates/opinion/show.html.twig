{% extends 'base.html.twig' %}

{% block title %}Opinion{% endblock %}

{% block body %}
    <h1>Opinion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ opinion.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ opinion.author }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ opinion.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary btn m-3" href="{{ path('opinion_index') }}">back to list</a>

    <a class="btn btn-primary btn m-3" href="{{ path('opinion_edit', {'id': opinion.id}) }}">edit</a>

    {{ include('opinion/_delete_form.html.twig') }}
{% endblock %}
