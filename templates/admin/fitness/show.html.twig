{% extends 'base.html.twig' %}

{% block title %}Fitness{% endblock %}

{% block body %}
    <h1>Fitness</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Texte partie Remise en forme</th>
            <td>{{ fitness.fitnessText }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('fitnessAdmin') }}">Retour</a>

    <a href="{{ path('fitness_edit', {'id': fitness.id}) }}">Modifier</a>

    {{ include('admin/fitness/_delete_form.html.twig') }}
{% endblock %}
