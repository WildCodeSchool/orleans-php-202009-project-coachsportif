{% extends 'base.html.twig' %}

{% block title %}CGV{% endblock %}

{% block body %}
    <h1>CGV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ c_g_v.id }}</td>
            </tr>
            <tr>
                <th>Document</th>
                <td>{{ c_g_v.document }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('c_g_v_index') }}">back to list</a>

    <a href="{{ path('c_g_v_edit', {'id': c_g_v.id}) }}">edit</a>

    {{ include('c_g_v/_delete_form.html.twig') }}
{% endblock %}
