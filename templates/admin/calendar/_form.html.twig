{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
    <button class="btn btn-primary btn m-3">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
