{% extends 'layout_admin.html.twig' %}
{% block title %}Ajout|Presentation{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mt-4">Ajout d'une presentation</h1>
        {{ include('admin/home/_form.html.twig') }}
        <div class="text-center">
            <a class="btn btn-secondary btn" href="{{ path('index_Admin') }}">Retour</a>
        </div>
    </div>
{% endblock %}
