<form method="post" action="{{ path('regulation_delete', {'id': regulation.id}) }}"
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer le texte ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ regulation.id) }}">
    <button class="btn btn-danger mb-sm-2 mb-2 btn">Supprimer</button>
</form>
