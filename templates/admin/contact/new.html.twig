{% extends 'layout_admin.html.twig' %}
{% block title %}Ajouter|Contact{% endblock %}

{% block body %}
<div class="container">
    <h1 class="title text-center mt-4">Ajouter des informations</h1>
    {{ include('admin/contact/_form.html.twig') }}
    <div class="text-center mt-4">
        <a class="btn btn-secondary" href="{{ path('contact_admin') }}">Retour</a>
    </div>
</div>
{% endblock %}
