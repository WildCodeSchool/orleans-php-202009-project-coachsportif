{% extends 'layout.html.twig' %}

{% block body %}
            <div class="fitnessContent">

                <h1 class="text-center">Remise en forme</h1>
                <div class="container-fluid">
                    {% for description in descriptions %}
                        <div class="row justify-content-center">
                            <p class="fitnessParagraph text-center col-8 py-2">{{ description.FitnessText }}</p>
                        </div>
                    {% endfor %}
                </div>
                <div class="container d-flex justify-content-center">
                    <div class="fitnessSubmitParagraph row">
                        <p>Si vous-êtes intéressé, il vous suffit de remplir un formulaire en ligne, simple et rapide
                            !</p>
                    </div>
                </div>
                <div class="d-flex justify-content-center">
                    <a class="fitnessButton btn btn-secondary btn-lg" href="#">S'inscrire</a>
                </div>
            </div>
{% endblock %}
