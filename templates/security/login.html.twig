{% extends 'base.html.twig' %}

{% block title %}Connectez-Vous!{% endblock %}
    {% block stylesheets %}
        {{ encore_entry_link_tags('security') }}
    {% endblock %}

{% block body %}
    <div class="container col-12 col-md-4">
        <div class="row justify-content-center">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {% if app.user %}
                    <div class="mb-3">
                        Tu es connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Se
                            déconnecter</a>
                    </div>
                {% endif %}

                <h1 class="h2 mb-3 font-weight-normal text-center ">Veuillez vous connecter</h1>
                    <label class="pt-4 px-1" for="inputEmail">E-mail</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail"
                           class="px-1 form-control justify-content-center" required autofocus>
                    <label class="pt-4 px-1" for="inputPassword">Mot de passe</label>
                    <input type="password" name="password" id="inputPassword" class="px-1 form-control"
                           required>

                    <input type="hidden" name="_csrf_token"
                           value="{{ csrf_token('authenticate') }}" class="px-1">

                <div class="checkbox mb-3">
                    <label class="pt-3 px-1">
                        <input type="checkbox" name="_remember_me"> Se souvenir de moi
                    </label>
                </div>

                <div class="text-center">
                    <button class="btn btn-secondary btn" type="submit">Se connecter</button>
                </div>
                <div class="text-center">
                    <a class="btn btn-primary btn m-3" type="submit" href="{{ path('app_register') }}">Créer un nouveau compte</a>
                </div>
            </form>
        </div>
    </div>
    <div class="text-right ">
        <a class="m-3 forgotPassword" type="submit"
           href="{{ path('app_forgot_password_request') }}">Mot de passe oublié ?</a>

    </div>
{% endblock %}

