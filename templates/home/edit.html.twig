{% extends 'base.html.twig' %}
    {% block stylesheets %}
        {{ encore_entry_link_tags('home') }}
    {% endblock %}
{% block title %}Modification|Présentation{% endblock %}

{% block body %}
    <div class="container text-center">
            <h1>Modification du texte et de l'image</h1>
            <div class="row justify-content-center">
                {{ include('home/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                <a class="btn btn-secondary btn-lg m-3" href="{{ path('index_Admin') }}">Retour</a>
                {{ include('home/_delete_form.html.twig') }}
            </div>
    </div>
{% endblock %}
