{% extends 'layout.html.twig' %}
{% block stylesheets %}
    {{ encore_entry_link_tags('home') }}
{% endblock %}
{% block title %}Home{% endblock %}

{% block body %}
    <h1>Damien Gouveia</h1>
    <div class="d-flex justify-content-center">
        <div class="homeTextPresentation">
                {% for text in home %}
                   <p class="homeText">{{ text.text }}</p>
                {% endfor %}
        </div>
    </div>
    <section class="homeContact">
        <div class="container-fluid bg-info text-light text-center col-12 py-4">
            <h2>Contact</h2>
        </div>
        <div class="cardContact container col-lg-3">
                <div class="card">
                    <h2 class="card-header text-light bg-info white-text text-center py-4">
                        <strong>Contactez nous</strong></h2>
                    <div class="card-body px-lg-2 pt-0">

                        <form class="text-center" style="color: #757575;" action="#!">
                            <div class="md-form mt-3">
                                <label for="materialContactFormName">Name</label>
                                <input type="text" id="materialContactFormName" class="form-control">
                            </div>
                            <div class="md-form">
                                <label for="materialContactFormEmail">E-mail</label>
                                <input type="email" id="materialContactFormEmail" class="form-control">
                            </div>
                            <div class="md-form">
                                <label for="materialContactFormMessage">Message</label>
                                <textarea id="materialContactFormMessage" class="form-control md-textarea" rows="3"></textarea>
                            </div>
                            <button class="btn btn-outline-info btn-rounded btn-block z-depth-0 my-4 waves-effect" type="submit">Send</button>
                        </form>
                    </div>
                </div>
        </div>
    </section>
{% endblock %}
