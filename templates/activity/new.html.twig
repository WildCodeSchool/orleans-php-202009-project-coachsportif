{% extends 'base.html.twig' %}

{% block title %}Ajouter dans la partie activitée adaptée{% endblock %}

{% block body %}
    <h1>Ajouter un texte </h1>

    {{ include('activity/_form.html.twig') }}
    <div class="container">
        <div class="row justify-content-end">
            <div class="col-3 mt-4">
                <a class="btn btn-primary" href="{{ path('activity_index') }}">Retour</a>
            </div>
        </div>
    </div>
{% endblock %}
