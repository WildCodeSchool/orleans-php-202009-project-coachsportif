{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-primary btn-lg m-3">{{ button_label|default('Enregistré') }}</button>
    </div>
{{ form_end(form) }}
